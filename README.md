# React Custom Hook to fetch API data

    In this project I've created a custom react hook named `useFetch`, it fetches the api and returns 3 states, { data, loading, error }.

## Commands

    npm install --> Install dependencies
    npm run dev --> Starts the app